---
title: 'Call History'
description: 'Search, filter, and review all your AI agent calls with advanced filtering and customization'
icon: 'phone'
---

# Call History

The Call History section provides a comprehensive view of all calls made by your AI agents. You can search, filter, and analyze call data to monitor performance and identify insights with powerful filtering capabilities and customizable views.

## Accessing Call History

Navigate to **Calls** in the main menu to access your complete call history.

## Interface Overview

The calls interface consists of:

| Section | Description |
|---------|-------------|
| 📊 Header | Title, description, and selection status |
| 🔍 Filter Bar | Advanced filtering controls with visibility management |
| 📋 Call Table | List of all calls with key information |
| 📄 Details Modal | Detailed view of individual calls |

## Advanced Filter System

### Filter Visibility Configuration

<Info>
Customize which filters are visible in your interface to streamline your workflow and focus on the most relevant filtering options.
</Info>

A dedicated **⚙️ settings button** is located at the end of the Filter Bar, next to the filter controls. It opens a configuration panel that lets you show or hide individual filters without affecting your applied filter values.

<Steps>
<Step title="Open Filter Settings">
Click the ⚙️ settings icon at the right end of the Filter Bar to open the filter visibility panel.
</Step>
<Step title="Toggle Individual Filters">
Use the checkboxes to show or hide each of the following filters independently:

- **Goal** — filter calls by the agent's configured goal or objective
- **Tags** — filter by dynamic or custom tags
- **Evaluations** — filter by evaluation completion or outcome
- **Call ID** — the direct database lookup search field

Core filters such as Date Range, Agents, Status, Direction, and Success are always visible and cannot be hidden, since they are considered essential for everyday use.
</Step>
<Step title="Apply and Close">
Close the panel by clicking outside of it or pressing the settings icon again. Hidden filters are immediately removed from the Filter Bar, freeing up space for the ones you actually use.
</Step>
<Step title="Automatic Persistence">
Your filter visibility preferences are saved automatically per user and restored the next time you open Call History — no need to reconfigure them on every visit.
</Step>
</Steps>

<Info>
By default, the **Tags** filter is now visible in the Filter Bar out of the box, so you don't need to open the settings panel to start filtering calls by tags. Goal, Evaluations, and Call ID remain part of the same visibility configuration and follow their own default state, which you can adjust at any time.
</Info>

<Tip>
If a filter is hidden but has an active value from a previous session, it will remain applied to your results even though it's not visible. Re-enable the filter from the settings panel to review or clear it.
</Tip>

### Filter Settings Panel (⚙️)

<Info>
The filter settings panel is the central place to control which optional filters appear in the Filter Bar. It's accessed through the gear icon located at the end of the filter controls row.
</Info>

The panel includes an individual toggle for each of the following filters:

| Filter | Default Visibility | Description |
|--------|--------------------|--------------|
| Goal | Hidden | Filter calls by the agent's configured objective |
| Tags | **Visible** | Filter by dynamic or custom tags applied to calls |
| Evaluations | Hidden | Filter by evaluation completion status (Completed, Pending, Partial) |
| Call ID | Hidden | Direct database lookup search field for a specific call |

<Steps>
<Step title="Click the Settings Icon">
Locate the ⚙️ gear icon at the end of the Filter Bar and click it to open the visibility panel.
</Step>
<Step title="Review Current State">
Each filter listed (Goal, Tags, Evaluations, Call ID) shows a checkbox reflecting whether it's currently shown or hidden in your Filter Bar.
</Step>
<Step title="Show or Hide a Filter">
Check a box to add that filter to the Filter Bar, or uncheck it to remove it from view. Changes apply instantly — there's no need to save or confirm.
</Step>
<Step title="Close the Panel">
Click anywhere outside the panel or click the gear icon again to close it. Your selections are preserved automatically for future sessions.
</Step>
</Steps>

<Tip>
Since the Tags filter now ships visible by default, most users will only need the settings panel to enable Goal, Evaluations, or Call ID when their workflow requires deeper filtering or precise lookups.
</Tip>

### Date Range Filter

Filter calls by predefined time periods or custom date ranges:

- **Today** - Calls from the current day
- **Yesterday** - Calls from the previous day
- **Last 7 Days** - Calls from the past week
- **Last 30 Days** - Calls from the past month
- **Custom Range** - Select specific start and end dates with time

<Info>
Use the datetime picker to set precise time ranges for your searches. The filter automatically adjusts to your local timezone.
</Info>

### Agent Filter with Evaluation Trends

Filter calls by specific agents (playbooks) with enhanced analytics integration:

1. Click the **Agents** dropdown
2. Select one or multiple agents
3. The filter shows "X agents selected" when active
4. Access **"View Evaluation Trends"** directly from the agent selector for performance analysis

<Tip>
When filtering by a single agent, the "View Evaluation Trends" button appears next to the agent name for quick access to performance analytics.
</Tip>

### Status Filter

Filter by call status with visual indicators:

| Status | Description | Indicator |
|--------|-------------|-----------|
| 🟢 Completed | Call finished successfully | Green badge |
| 🟡 In Progress | Call currently active | Yellow badge |
| 🔴 Failed | Call encountered an error | Red badge |
| ⚪ No Answer | Recipient didn't answer | White badge |
| 🔵 Transferred | Call was transferred | Blue badge |

### Direction Filter

- **Inbound** - Calls received by your agents with 📞 icon
- **Outbound** - Calls initiated by your agents with 📲 icon
- **All** - Both directions

### Success Filter

Filter by call outcome with enhanced evaluation criteria:

- **Successful** - Calls marked as successful based on your evaluations
- **Unsuccessful** - Calls that didn't meet success criteria
- **All** - All calls regardless of outcome

### Goal Filter

<Info>
The Goal filter lets you narrow down calls by the objective configured on the agent, making it easier to review calls tied to a specific business intent (e.g., booking, qualification, support resolution).
</Info>

This filter can be shown or hidden from the filter visibility panel described above. It is hidden by default; enable it from the ⚙️ settings panel if you need to filter calls by their configured goal.

### Advanced Call ID Search

<Warning>
When using Call ID search, all other filters are temporarily disabled to ensure precise lookups.
</Warning>

The Call ID filter has **priority over every other filter** in the Filter Bar. As soon as you start typing a value into the Call ID field, all remaining filters (Date Range, Agents, Status, Direction, Success, Goal, Tags, and Evaluations) become visually disabled and are excluded from the query — the interface greys them out and blocks interaction with them to make it clear that only the Call ID lookup is active.

Enhanced Call ID functionality with database search:

<Steps>
<Step title="Enter Call ID">
Type the specific call ID in the dedicated search field. As soon as a value is present, the rest of the filters switch to a disabled visual state.
</Step>
<Step title="Database Query">
The system performs a direct database lookup for the exact call ID, ignoring any values currently set in other filters.
</Step>
<Step title="Instant Results">
The matching call is displayed immediately, bypassing all other filters.
</Step>
<Step title="Clear Search">
Remove the call ID to restore normal filtering functionality. All previously configured filters are automatically re-enabled and reapplied.
</Step>
</Steps>

<Tip>
Like the Goal and Evaluations filters, the Call ID field is hidden from the Filter Bar by default and can be enabled from the filter visibility settings (⚙️) whenever your workflow requires manual ID lookups.
</Tip>

### Dynamic Tags Filter

<Info>
Filter calls by dynamic tags that are automatically generated based on call content, outcomes, and agent behaviors.
</Info>

The tags filter provides:

- **Dynamic Tag Generation** - Tags are automatically created based on call analysis
- **Multi-Select Capability** - Select multiple tags to find calls matching any or all criteria
- **Real-time Updates** - Tag list updates as new calls are processed
- **Custom Tag Support** - Manual tags can be added to calls for additional categorization

<Info>
The Tags filter is now **visible by default** in the Filter Bar, so it's ready to use as soon as you open Call History. You can still hide it from the ⚙️ filter settings panel if you prefer a more minimal filter set. Like all other filters, it is temporarily disabled while a Call ID search is active.
</Info>

### Evaluations Filter

<Info>
Filter calls based on evaluation status, letting you quickly locate calls that are pending review, partially evaluated, or fully completed.
</Info>

Available options include:

- **Completed** - Calls with all evaluations processed
- **Pending** - Calls waiting for evaluation
- **Partial** - Calls with some but not all evaluations completed

This filter is hidden by default in a minimal setup and can be enabled from the filter visibility panel (⚙️) whenever you need deeper evaluation-based analysis. It is also disabled while a Call ID search is active, following the same priority behavior as the rest of the filters.

### Filter Presets

<Tip>
Save your most-used filter combinations as presets for quick access to common search patterns.
</Tip>

Create and manage filter presets:

<Steps>
<Step title="Configure Filters">
Set up your desired combination of filters (date range, agents, status, etc.)
</Step>
<Step title="Save as Preset">
Click "Save Preset" and give it a descriptive name
</Step>
<Step title="Quick Access">
Access saved presets from the preset dropdown for instant filter application
</Step>
<Step title="Manage Presets">
Edit or delete presets as your filtering needs evolve
</Step>
</Steps>

## Enhanced Visual Indicators

### Filter Status Indicators

The interface now includes improved visual feedback:

| Indicator | Meaning |
|-----------|---------|
| 🟦 Blue Badge | Active filter with values selected |
| 🔢 Count Badge | Number of items selected in multi-select filters |
| 🚫 Disabled State | Filter temporarily disabled (e.g., during Call ID search) |
| ⚙️ Settings Icon | Opens the filter visibility configuration panel (controls Goal, Tags, Evaluations, and Call ID) |
| 👁️ Hidden Filter | Filter is configured as not visible in the current view |
| ✨ Preset Active | A saved preset is currently applied |

### Selection Status

When calls are selected:

- **Selection Counter** - Shows "X selected" in an emerald-colored badge
- **Bulk Action Bar** - Appears with available actions for selected calls
- **Clear Selection** - One-click option to deselect all calls

## Call Table

The call table displays key information for each call with enhanced visual design:

| Column | Description |
|--------|-------------|
| Selection | Checkbox to select calls |
| Status | Call status badge with color coding |
| Success | ✓ or ✗ indicator with evaluation context |
| Agent | Name of the agent that handled the call |
| Direction | 📞 Inbound or 📲 Outbound with clear icons |
| From | Caller phone number with formatting |
| To | Recipient phone number with formatting |
| Duration | Call length in minutes:seconds |
| Date | When the call occurred with relative time |
| Tags | Dynamic tags associated with the call |
| LLM *(optional)* | Language model(s) that handled the call |
| LLM detail *(optional)* | Turn-by-turn breakdown of model usage |

<Info>
The **LLM** and **LLM detail** columns are optional and hidden by default. Enable them from the column customizer to monitor which language models powered each call.
</Info>

### LLM and LLM Detail Columns

<Info>
These two columns surface the underlying language model activity for every call, which is especially useful for agents built on ElevenLabs' conversational stack, where a **model cascade** can automatically fall back to a secondary model mid-call if the primary one becomes unavailable or degrades.
</Info>

| Column | What it shows |
|--------|---------------|
| **LLM** | The distinct model(s) used during the call, listed in the order they were engaged (e.g. a single model like `gpt-4o`, or multiple models such as `gpt-4o + gpt-4o-mini` if a fallback occurred). |
| **LLM detail** | An expandable, turn-by-turn breakdown showing exactly which model handled each conversational turn and at what point in the call the switch happened. |

<Steps>
<Step title="Enable the Columns">
Open the column customizer above the Call Table and toggle on **LLM** and/or **LLM detail** to add them to your view.
</Step>
<Step title="Read the LLM Column">
Each row shows the model(s) used for that call. A single model name means the call ran entirely on the primary model with no fallback.
</Step>
<Step title="Spot Degradations">
When a call shows **more than one model** in the LLM column, it means the ElevenLabs cascade degraded at least once during the call — falling back from the primary model to a secondary one. This is the same detection logic used in the call details modal.
</Step>
<Step title="Drill Into LLM Detail">
Expand the LLM detail column (or open the call in the Details Modal) to see the exact timestamp of each turn and which model produced it, helping you pinpoint when and why a degradation happened.
</Step>
</Steps>

<Tip>
Use the LLM column together with the Status and Success filters to quickly check whether cascade degradations correlate with lower success rates or specific agents, then dig deeper with LLM detail on the affected calls.
</Tip>

<Warning>
For calls made before model cascade tracking was introduced, the LLM column falls back to the legacy single `model_used` value when no multi-model data is available. In that case, no degradation can be detected for those older calls.
</Warning>

### Bulk Actions

<Tabs>
<Tab title="Selection">
When you select multiple calls:
1. A selection indicator appears showing "X selected"
2. Click **Clear Selection** to deselect all
3. Bulk export options become available
4. Additional bulk actions appear in the action bar
</Tab>
<Tab title="Export">
Export filtered results:
- CSV format for spreadsheet analysis
- JSON format for technical analysis
- PDF reports for presentations
- Include/exclude specific columns
</Tab>
</Tabs>

### Enhanced Sorting

Click column headers to sort with improved indicators:

- **Single Click** - Ascending order with ↑ arrow
- **Double Click** - Descending order with ↓ arrow
- **Sort Memory** - Last sort preference is remembered
- **Multi-Column Sort** - Hold Shift to sort by multiple columns

## Pagination

Enhanced pagination controls:

- **Smart Page Sizes** - 10, 25, 50, 100, 250 calls per page
- **Jump to Page** - Direct page number input
- **Results Counter** - "Showing X to Y of Z calls" with total count
- **Performance Optimization** - Efficient loading for large datasets

## Quick Actions

For each call row with improved accessibility:

| Action | Description | Shortcut |
|--------|-------------|----------|
| 👁️ View | Open call details modal | Click row |
| 📋 Copy ID | Copy call ID to clipboard | Right-click menu |
| 🏷️ Add Tags | Add custom tags to the call | Tag icon |
| 📊 Analytics | Quick performance insights | Chart icon |

## Evaluation Trends Analysis

<Info>
Access comprehensive evaluation trends analysis directly from the call history interface to track agent performance patterns over time.
</Info>

### Accessing Evaluation Trends

From the agent dropdown in your call filters:

1. Select the agent you want to analyze
2. Look for the **"View Evaluation Trends"** button next to the agent name
3. Click to open the evaluation trends dashboard for that specific agent

### What You'll Find

The evaluation trends analysis provides:

- **Long-term Performance Tracking** - Monitor how your agent's success rates evolve over weeks and months
- **Success Rate Patterns** - Identify trends, improvements, or areas needing attention
- **Historical Context** - Compare current performance with past periods
- **Actionable Insights** - Understand which evaluations are performing well and which need optimization

## Performance and Optimization

### Smart Loading

The system includes several performance optimizations:

- **Lazy Loading** - Calls load as you scroll through the table
- **Filter Debouncing** - Prevents excessive API calls while typing
- **Caching** - Frequently accessed data is cached for faster loading
- **Background Refresh** - New calls appear automatically without manual refresh

### Search Optimization

<Tip>
Use specific search criteria to improve performance when working with large datasets.
</Tip>

Best practices for efficient searching:

1. **Start with Date Range** - Narrow down the time period first
2. **Use Specific Agents** - Filter by particular agents when possible
3. **Combine Filters Strategically** - Use multiple filters to reduce result sets
4. **Save Common Searches** - Use presets for frequently used filter combinations

## Tips for Efficient Searching

<CardGroup cols={2}>
  <Card title="Use Filter Presets" icon="bookmark">
    Save your most common filter combinations as presets for instant access to frequently needed views.
  </Card>
  <Card title="Leverage Dynamic Tags" icon="tags">
    Use the Tags filter — visible by default in the Filter Bar — to quickly find calls with specific characteristics or outcomes.
  </Card>
  <Card title="Combine Multiple Filters" icon="filter">
    Stack filters like "Failed + Outbound + Last 24h + Specific Agent" for precise targeting.
  </Card>
  <Card title="Use Call ID for Debugging" icon="hashtag">
    When investigating specific issues, enable the Call ID filter from settings and use the exact call ID for instant lookup with database search. Remember it temporarily overrides all other filters.
  </Card>
  <Card title="Customize Filter Visibility" icon="eye">
    Use the ⚙️ settings button to show or hide Goal, Tags, Evaluations, and Call ID filters, keeping the Filter Bar focused on what matters to you.
  </Card>
  <Card title="Export Filtered Results" icon="download">
    Export your filtered results for offline analysis, reporting, or sharing with team members.
  </Card>
  <Card title="Track LLM Cascade Health" icon="microchip">
    Enable the LLM and LLM detail columns to spot ElevenLabs cascade degradations and understand which calls fell back to a secondary model.
  </Card>
</CardGroup>

## Advanced Use Cases

### Quality Assurance Workflow

<Steps>
<Step title="Create QA Preset">
Save a filter preset for "Failed + Last 7 Days" to quickly review problematic calls.
</Step>
<Step title="Tag Review Calls">
Use the Tags filter — already visible in the Filter Bar by default — to identify calls that need quality review.
</Step>
<Step title="Bulk Analysis">
Select multiple calls for bulk export and detailed analysis.
</Step>
<Step title="Track Improvements">
Use evaluation trends to monitor quality improvements over time.
</Step>
</Steps>

### Performance Monitoring

<Steps>
<Step title="Agent Comparison">
Use the agent filter to compare performance across different agents.
</Step>
<Step title="Success Tracking">
Filter by success/failure to identify patterns in call outcomes.
</Step>
<Step title="Trend Analysis">
Access evaluation trends directly from agent filters for deep performance insights.
</Step>
<Step title="Regular Reviews">
Set up filter presets for weekly/monthly performance reviews.
</Step>
</Steps>

### Targeted Debugging with Call ID

<Steps>
<Step title="Enable the Call ID Filter">
Open the ⚙️ filter settings panel and enable the Call ID filter if it isn't already visible in your Filter Bar.
</Step>
<Step title="Locate the Call ID">
Copy the call ID from a support ticket, log entry, or the Quick Actions "Copy ID" option.
</Step>
<Step title="Paste into Call ID Search">
Enter the ID into the Call ID field. All other filters are immediately disabled to avoid conflicting results.
</Step>
<Step title="Review the Exact Match">
The system performs a direct database lookup and returns the single matching call, regardless of any date range or agent filter previously applied.
</Step>
<Step title="Clear to Resume">
Clear the Call ID field once you're done to automatically restore your previous filter configuration.
</Step>
</Steps>

### Investigating LLM Cascade Degradations

<Steps>
<Step title="Enable the LLM Columns">
Open the column customizer and turn on **LLM** and **LLM detail** for your Call Table view.
</Step>
<Step title="Scan for Multiple Models">
Look for rows where the LLM column lists more than one model — this flags a cascade degradation during that call.
</Step>
<Step title="Open LLM Detail">
Expand LLM detail (or open the Details Modal) to see the exact turn where the switch happened and which model took over.
</Step>
<Step title="Cross-Reference Outcomes">
Combine this with the Success and Status filters to check whether degraded calls correlate with lower success rates.
</Step>
</Steps>

## Next Steps

<CardGroup cols={2}>
  <Card title="Call Details" icon="info-circle" href="/calls/call-details">
    Learn about the detailed information available for each call including transcripts and analysis.
  </Card>
  <Card title="Evaluation Trends" icon="chart-line" href="/calls/evaluation-trends">
    Analyze agent performance patterns over time with comprehensive trend analysis.
  </Card>
  <Card title="Reporting" icon="chart-bar" href="/calls/reporting">
    Generate comprehensive reports from your call data with advanced analytics.
  </Card>
  <Card title="Filter Management" icon="settings" href="/calls/filter-management">
    Advanced filter configuration and preset management options.
  </Card>
</CardGroup>