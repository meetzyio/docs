---
title: 'Detalles de Llamada'
description: 'Ver transcripción, grabación, datos de entrada/salida y más para cada llamada'
icon: 'file-lines'
---

# Detalles de Llamada

Cada llamada genera datos detallados que te ayudan a entender qué sucedió durante la conversación. El modal de Detalles de Llamada proporciona acceso a toda la información capturada.

## Abrir Detalles de Llamada

Haz clic en cualquier fila de la tabla de Historial de Llamadas, o haz clic en el botón **View** para abrir el modal de detalles.

## Diseño del Modal

El modal de Detalles está organizado en un layout de tres columnas:

| Panel | Contenido |
|-------|-----------|
| ⬅️ Izquierda | Parámetros de Entrada (JSON) |
| 📊 Centro | Información de la Llamada |
| ➡️ Derecha | Respuesta de Salida (JSON) |

## Parámetros de Entrada

El panel izquierdo muestra el JSON de entrada enviado para iniciar la llamada:

```json
{
  "phone": "+1234567890",
  "customer_name": "Juan García",
  "appointment_date": "2024-02-15",
  "custom_fields": {
    "order_id": "ORD-12345"
  }
}
```

### Funcionalidades

- **Botón Copiar** - Copia todo el JSON al portapapeles
- **Resaltado de Sintaxis** - JSON con colores para legibilidad
- **Expandible** - Scroll para payloads grandes

## Información de la Llamada

El panel central muestra metadatos clave:

| Campo | Descripción |
|-------|-------------|
| Call ID | Identificador único (monoespaciado, copiable) |
| Status | Estado actual con badge de color |
| Success | Indicador ✓ Sí o ✗ No |
| Playbook | Nombre del agente que manejó la llamada |
| Duration | Duración en formato legible |
| From | Número de teléfono origen |
| To | Número de teléfono destino |
| Email | Email del contacto (si se proporcionó) |
| Created Time | Timestamp de inicio de la llamada |
| Tags | Etiquetas aplicadas a esta llamada |

### Badges de Estado

| Estado | Color | Descripción |
|--------|-------|-------------|
| completed | 🟢 Verde | Llamada terminada normalmente |
| in-progress | 🔵 Azul | Llamada actualmente activa |
| failed | 🔴 Rojo | Llamada con error |
| transferred | 🟣 Morado | Llamada transferida |
| no-answer | ⚪ Gris | El destinatario no contestó |

## Bloque LLM

<Info>
Este bloque solo aparece si la llamada tiene datos de uso de modelos LLM registrados. Las llamadas anteriores al 29 de julio de 2026 pueden mostrar un único modelo mediante un campo de compatibilidad.
</Info>

El panel central incluye una sección **LLM** que muestra qué modelo (o modelos) de lenguaje procesó la conversación. Meetzy utiliza un sistema de cascada de proveedores (por ejemplo, ElevenLabs) que puede degradar automáticamente a un modelo alternativo si el modelo principal no está disponible o presenta latencia elevada.

### Qué muestra

| Elemento | Descripción |
|----------|-------------|
| Modelos usados | Lista de los modelos distintos utilizados durante la llamada, en orden de aparición |
| Indicador de degradación | Se activa cuando se detecta más de un modelo distinto en la misma llamada |
| Detalle por turno | Desglose opcional que muestra qué modelo respondió en cada turno de la conversación |

### Indicador de Degradación

<Warning>
Cuando una llamada usa más de un modelo LLM, se considera que hubo una **degradación del cascade**: el modelo principal no pudo atender la solicitud en algún punto y el sistema recurrió a un modelo alternativo para mantener la llamada en curso.
</Warning>

Este indicador te ayuda a identificar rápidamente llamadas que pudieron verse afectadas por cambios de modelo, útil para:

- Detectar problemas de disponibilidad con un proveedor específico
- Correlacionar degradaciones con caídas de calidad en la conversación
- Priorizar la revisión manual de llamadas con cascade activo

### Detalle por Turno

Por defecto, el detalle por turno aparece **plegado**. Al expandirlo, verás una tabla con:

| Campo | Descripción |
|-------|-------------|
| Turno | Marca de tiempo (en segundos) dentro de la llamada |
| Modelo(s) | Modelo o combinación de modelos usados en ese turno |
| Tokens de salida | Cantidad de tokens generados por el modelo en ese turno |

<Steps>
<Step title="Localiza el bloque LLM">
En el panel central del modal de detalles, busca la sección **LLM** debajo de la información básica de la llamada.
</Step>
<Step title="Revisa los modelos usados">
Verás la lista de modelos distintos detectados en la llamada. Si hay más de uno, aparecerá el indicador de degradación.
</Step>
<Step title="Expande el detalle por turno">
Haz clic para desplegar la tabla de turnos y ver exactamente en qué momento ocurrió el cambio de modelo.
</Step>
</Steps>

<Info>
Esta información es especialmente útil junto con el botón **"Analyze with AI"**: puedes pedirle al Copilot que correlacione una degradación de modelo con caídas de sentimiento o errores en la conversación.
</Info>

## Respuesta de Salida

El panel derecho muestra el JSON de salida con todos los datos capturados:

```json
{
  "transcription": "Agente: Hola, soy Sara de...",
  "summary": "El cliente confirmó la cita para...",
  "sentiment": "positive",
  "output_params": {
    "appointment_confirmed": true,
    "preferred_time": "morning",
    "callback_requested": false
  },
  "evaluations": {
    "successful_greeting": true,
    "collected_requirements": true
  }
}
```

### Contenido de Salida

<AccordionGroup>
  <Accordion title="Transcripción">
    Transcripción completa de la conversación entre el agente y el llamante, con etiquetas de hablante.
  </Accordion>
  <Accordion title="Resumen">
    Resumen generado por IA de los puntos clave y resultados de la llamada.
  </Accordion>
  <Accordion title="Parámetros de Salida">
    Datos estructurados extraídos de la conversación según tu configuración de Output Settings.
  </Accordion>
  <Accordion title="Evaluaciones">
    Resultados de evaluaciones automáticas definidas en la configuración del agente.
  </Accordion>
  <Accordion title="Tool Calls">
    Si el agente invocó herramientas o webhooks, sus peticiones y respuestas se incluyen.
  </Accordion>
</AccordionGroup>

## Navegación al Copilot

<Info>
Desde los detalles de cualquier llamada, puedes navegar directamente al Copilot de IA con el contexto de la conversación precargado.
</Info>

### Acceso Contextual

En el panel central de información de la llamada, encontrarás un botón **"Analyze with AI"** que te permite:

<Steps>
<Step title="Navegación Contextual">
Haz clic en **"Analyze with AI"** para abrir el Copilot con el contexto de la llamada
</Step>
<Step title="Contexto Precargado">
El Copilot se abrirá automáticamente con referencia `@call:<id>` que incluye toda la transcripción
</Step>
<Step title="Análisis Inteligente">
Realiza consultas como "Analiza el sentimiento de esta llamada" o "Genera un resumen ejecutivo"
</Step>
</Steps>

### Casos de Uso del Copilot

<CardGroup cols={2}>
  <Card title="Análisis de Sentimiento" icon="heart">
    Evalúa el tono emocional de la conversación y la satisfacción del cliente
  </Card>
  <Card title="Extracción de Insights" icon="lightbulb">
    Identifica patrones, objeciones comunes y oportunidades de mejora
  </Card>
  <Card title="Optimización de Prompts" icon="wand-magic-sparkles">
    Mejora los prompts del agente basándose en conversaciones reales
  </Card>
  <Card title="Generación de Reportes" icon="chart-bar">
    Crea resúmenes ejecutivos y análisis detallados de rendimiento
  </Card>
</CardGroup>

### Flujo de Trabajo Integrado

El sistema proporciona una experiencia fluida entre el análisis de llamadas y la optimización de agentes:

1. **Revisar Llamada** - Examina transcripción y métricas en el modal de detalles
2. **Analizar con IA** - Un clic te lleva al Copilot con contexto completo
3. **Optimizar Agente** - Aplica mejoras directamente al agente desde el Copilot
4. **Validar Cambios** - Regresa al historial para verificar el impacto

## Información de Webhook

Si tu agente tiene un webhook configurado, la parte inferior del panel derecho muestra:

| Campo | Valor |
|-------|-------|
| Method | POST, GET, etc. |
| URL | Endpoint del webhook |
| Auth | Header de autorización (oculto) |

<Info>
La sección de webhook solo aparece si se configuró y ejecutó un webhook para esta llamada.
</Info>

## Reproductor de Grabación y Transcripción Enriquecida

Si la grabación de llamadas está habilitada, desde la columna **Recording** de la tabla de llamadas (o desde el botón correspondiente en los Detalles de Llamada) puedes abrir un reproductor avanzado con la grabación y, junto a ella, una vista enriquecida de la transcripción turno por turno.

El modal se divide en dos zonas:

| Zona | Contenido |
|------|-----------|
| 🎧 Izquierda | Reproductor de audio con forma de onda |
| 💬 Derecha | Transcripción turno por turno con desglose de latencia, modelo LLM y tool calls |

### Reproductor de Audio

El reproductor dibuja la **forma de onda real** del audio (decodificada con la Web Audio API a partir del archivo descargado, no una animación genérica), lo que te permite ubicar visualmente silencios, picos de voz y pausas. A medida que la llamada avanza, la parte ya reproducida de la forma de onda se resalta en color para indicar el progreso.

<Steps>
<Step title="Reproduce o pausa">
Usa el botón central de play/pause, o haz clic directamente sobre la forma de onda para saltar a ese punto exacto de la llamada.
</Step>
<Step title="Salta 10 segundos">
Los botones ⏪ y ⏩ retroceden o avanzan 10 segundos, útiles para revisar rápidamente un momento específico.
</Step>
<Step title="Ajusta la velocidad">
Elige entre 0.5x, 0.75x, 1x, 1.25x, 1.5x o 2x para escuchar la llamada más rápido o más despacio.
</Step>
<Step title="Descarga la grabación">
Usa el enlace **"Download recording"** para guardar el archivo de audio localmente.
</Step>
</Steps>

<Warning>
Las grabaciones de llamadas se almacenan por tiempo limitado según tu plan. Descarga grabaciones importantes para almacenamiento permanente.
</Warning>

### Navegación entre Llamadas

<Info>
El reproductor de grabación no es una ventana aislada por llamada: puedes moverte a la llamada anterior o siguiente de la lista filtrada sin cerrar el modal ni perder tu posición en la tabla.
</Info>

En la cabecera del modal, junto al botón de cerrar, aparecen dos flechas de navegación:

| Control | Acción |
|---------|--------|
| ⬅️ Flecha izquierda | Abre la grabación y transcripción de la **llamada anterior** en la lista actual |
| ➡️ Flecha derecha | Abre la grabación y transcripción de la **siguiente llamada** en la lista actual |

<Steps>
<Step title="Navega con los botones de cabecera">
Haz clic en ⬅️ o ➡️ para cargar la llamada anterior o siguiente respecto a la que tenías abierta, respetando el orden y los filtros aplicados en la tabla.
</Step>
<Step title="O usa las flechas del teclado">
Con el modal abierto, presiona `←` o `→` para navegar sin soltar el mouse. Esto no funciona mientras estás escribiendo dentro de un campo de texto, como la nota de revisión o el input de etiquetas.
</Step>
<Step title="Observa la transición">
Al cambiar de llamada, el contenido se desliza suavemente en la dirección correspondiente, confirmando visualmente que se cargó una llamada distinta.
</Step>
</Steps>

<Info>
Las flechas se deshabilitan automáticamente cuando no hay una llamada anterior o siguiente disponible (por ejemplo, en el primer o último registro de la página actual).
</Info>

Esta navegación es especialmente útil para sesiones de **control de calidad**: puedes revisar de forma continua un lote de llamadas —escuchando audio, leyendo la transcripción, marcando como revisada y agregando notas o etiquetas— sin volver constantemente a la tabla principal.

### Transcripción Enriquecida por Turno

Junto al reproductor, cada turno de la conversación (agente o usuario) se muestra como una burbuja de chat, con indicadores debajo que resumen lo que ocurrió técnicamente en ese turno:

| Indicador | Aparece en | Descripción |
|-----------|------------|--------------|
| Marca de tiempo | Todos los turnos | Minuto:segundo dentro de la llamada |
| `Transcribed in Xms` | Turnos del usuario | Latencia del reconocimiento de voz (ASR / speech-to-text) |
| `Replied in Xs` | Turnos del agente | Tiempo total de respuesta del agente, expandible en un desglose de latencia |
| 🔧 Nombre de herramienta | Turnos del agente con tool calls | Una por cada herramienta invocada en ese turno, expandible |

<Info>
Estos datos provienen directamente de la conversación original en ElevenLabs (`conversation_turn_metrics`, `llm_usage`, `tool_calls` y `tool_results`), replicando el nivel de detalle que verías en la consola del proveedor, ya que Meetzy no persiste esta granularidad turno por turno en su propia base de datos.
</Info>

#### Desglose de latencia por turno ("Replied in")

Al hacer clic en el indicador **"Replied in Xs"** de un turno del agente, se despliega un panel visual que responde a la pregunta "¿en qué se fue ese tiempo?" en lugar de mostrar métricas crudas sin contexto:

- **Tiempo de respuesta total** - el número principal, destacado en grande
- **Modelo** - el modelo de lenguaje que generó la respuesta en ese turno
- **Barra apilada de composición** - una barra de progreso dividida en segmentos de color, cada uno proporcional al tiempo que ocupó dentro del total
- **Lista de segmentos** - el detalle de cada etapa, con su duración exacta y su porcentaje sobre el total:

| Segmento | Color | Descripción |
|----------|-------|--------------|
| Understanding speech (ASR) | 🔵 Azul | Tiempo del motor de reconocimiento de voz en transcribir lo que dijo el usuario |
| Model thinking (LLM) | 🟣 Morado | Tiempo hasta el primer byte generado por el modelo de lenguaje |
| Generating voice (TTS) | 🟢 Verde | Tiempo del servicio de texto a voz en producir el audio de la respuesta |
| Other overhead | ⚪ Gris | Diferencia residual entre la suma de etapas y el tiempo total medido |
| Waiting on purpose | ⚫ Gris oscuro | Tiempo que el agente esperó deliberadamente antes de hablar (por ejemplo, antes de preguntar "¿sigues ahí?") |

<Info>
El segmento **"Waiting on purpose"** no representa lentitud del sistema: es una pausa intencional del agente. Por eso se muestra por separado, en una nota debajo del desglose, en lugar de sumarse al tiempo de respuesta destacado. Esto evita que una espera deliberada de varios segundos haga parecer que la latencia real del pipeline es mucho peor de lo que es.
</Info>

<Steps>
<Step title="Ubica el turno del agente">
En la transcripción, busca el indicador **"Replied in Xs"** debajo del mensaje del agente que quieres analizar.
</Step>
<Step title="Expande el desglose">
Haz clic en el indicador para desplegar el panel con la barra apilada y la lista de segmentos.
</Step>
<Step title="Identifica el cuello de botella">
Compara los porcentajes de cada segmento (ASR, LLM, TTS, overhead) para saber en qué etapa se concentró la mayor parte del tiempo de respuesta.
</Step>
</Steps>

#### Detalle de Tool Calls por turno

Al hacer clic en el indicador 🔧 de una herramienta, se despliega un panel con:

- **Parámetros** - el JSON enviado a la herramienta en el momento de la invocación
- **Respuesta** - el JSON devuelto por la herramienta, marcado como error si corresponde

<Steps>
<Step title="Abre el reproductor">
Haz clic en el ícono de reproducción en la columna **Recording**, o desde el botón de grabación dentro de los Detalles de Llamada.
</Step>
<Step title="Ubica el turno de interés">
Desplázate por la transcripción a la derecha mientras escuchas el audio a la izquierda.
</Step>
<Step title="Expande el desglose">
Haz clic en **"Replied in Xs"** o en cualquier tool call para ver el detalle completo de ese turno.
</Step>
</Steps>

### Transcripción de Respaldo

<Info>
Si la conversación original en ElevenLabs ya no está disponible (por ejemplo, llamadas muy antiguas), el modal muestra automáticamente una **transcripción de texto plano**, sin los indicadores de latencia ni el desglose por turno, indicando que las métricas detalladas ya no pueden recuperarse.
</Info>

## Copiar Datos

### Botones de Copiar

- **Copy Input** - Copia todo el JSON de entrada
- **Copy Output** - Copia todo el JSON de salida
- **Copy Call ID** - Copia solo el identificador de llamada

### Feedback

Cuando copias datos:
- El botón cambia a "Copied!" con un checkmark
- Vuelve a la normalidad después de 2 segundos

## Casos de Uso Comunes

<CardGroup cols={2}>
  <Card title="Debugging" icon="bug">
    Revisa params de entrada y salida para diagnosticar problemas.
  </Card>
  <Card title="Control de Calidad" icon="check-double">
    Escucha grabaciones y verifica transcripciones para asegurar calidad.
  </Card>
  <Card title="Verificación de Integración" icon="plug">
    Verifica que los datos del webhook se enviaron correctamente.
  </Card>
  <Card title="Datos de Entrenamiento" icon="graduation-cap">
    Exporta detalles de llamadas para mejorar prompts de agentes.
  </Card>
</CardGroup>

## Atajos de Teclado

### Modal de Detalles

| Atajo | Acción |
|-------|--------|
| `Esc` | Cerrar el modal |
| Clic fuera | Cerrar el modal |

### Modal de Grabación

| Atajo | Acción |
|-------|--------|
| `←` | Ir a la llamada anterior de la lista |
| `→` | Ir a la siguiente llamada de la lista |
| `Esc` | Cerrar el modal |

<Info>
Los atajos de flecha izquierda/derecha no se activan mientras el foco está en un campo de texto (por ejemplo, al escribir una nota de revisión o una etiqueta).
</Info>

## Siguientes Pasos

<CardGroup cols={2}>
  <Card title="Historial de Llamadas" icon="list" href="/es/calls/call-history">
    Volver a buscar y filtrar llamadas.
  </Card>
  <Card title="Reportes" icon="chart-line" href="/es/calls/reporting">
    Genera reportes de tus datos de llamadas.
  </Card>
</CardGroup>