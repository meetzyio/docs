---
title: 'Parámetros de Entrada'
description: 'Configura variables dinámicas para personalizar cada llamada'
icon: 'arrow-right-to-bracket'
---

## ¿Qué son los Parámetros de Entrada?

Los Parámetros de Entrada son variables dinámicas que te permiten personalizar cada llamada con información específica del cliente. Al iniciar una llamada saliente o recibir una llamada entrante a través de tu API, puedes pasar estas variables para personalizar el comportamiento y conocimiento del agente.

<Info>
Los Parámetros de Entrada se pasan vía API al iniciar llamadas, haciendo cada conversación única y personalizada.
</Info>

## Sugerencias del Copiloto de IA

El Copiloto de IA puede analizar tu prompt del sistema y sugerir automáticamente parámetros de entrada relevantes para mejorar la personalización de tu agente.

### Cómo Funciona

Cuando escribes o modificas el prompt del sistema, el Copiloto de IA:

1. **Analiza el Contexto** - Examina las tareas y responsabilidades descritas en el prompt
2. **Identifica Variables** - Detecta información que podría variar entre llamadas
3. **Sugiere Parámetros** - Propone parámetros con nombres, tipos y descripciones apropiadas

### Banner de Sugerencias

Cuando hay sugerencias pendientes, aparecerá un banner púrpura en la parte superior de la sección:

<Info>
**Copiloto Sugerencias** - Muestra el número de parámetros sugeridos con opciones para "Aceptar Todo" o "Rechazar"
</Info>

### Tipos de Sugerencias

| Operación | Descripción | Ejemplo |
|-----------|-------------|---------|
| **Agregar** | Nuevos parámetros sugeridos | `nombre_cliente`, `numero_pedido` |
| **Modificar** | Cambios a parámetros existentes | Cambiar tipo de `string` a `number` |
| **Remover** | Parámetros que ya no son necesarios | Parámetros obsoletos o duplicados |

## Casos de Uso Comunes

| Caso de Uso | Parámetros de Ejemplo |
|-------------|----------------------|
| Identificación del Cliente | `nombre_cliente`, `id_cliente` |
| Reserva de Citas | `fecha_cita`, `hora_cita`, `tipo_servicio` |
| Estado de Pedido | `numero_pedido`, `estado_pedido`, `fecha_entrega` |
| Gestión de Cuenta | `saldo_cuenta`, `fecha_vencimiento_pago` |
| Calificación de Lead | `fuente_lead`, `interes_producto` |

## Configurando Parámetros de Entrada

### Añadiendo un Parámetro

<Steps>
  <Step title="Abrir Sección de Parámetros de Entrada">
    Navega a la sección de Parámetros de Entrada en el panel izquierdo.
  </Step>
  <Step title="Añadir Nuevo Parámetro">
    Haz clic en "Añadir Parámetro" para crear una nueva variable.
  </Step>
  <Step title="Configurar Propiedades">
    Establece el nombre, tipo y descripción.
  </Step>
</Steps>

### Usar Plantillas

Las plantillas te ayudan a añadir rápidamente conjuntos comunes de parámetros:

<Steps>
  <Step title="Abrir Plantillas">
    Haz clic en el botón "Plantillas" para ver las opciones disponibles.
  </Step>
  <Step title="Seleccionar Plantilla">
    Elige una plantilla que se ajuste a tu caso de uso (ej., "Información del Cliente").
  </Step>
  <Step title="Aplicar Plantilla">
    Los parámetros se añadirán automáticamente a tu configuración.
  </Step>
</Steps>

### Propiedades del Parámetro

| Propiedad | Descripción | Requerido |
|-----------|-------------|-----------|
| Nombre | Identificador de variable (ej., `nombre_cliente`) | Sí |
| Tipo | Tipo de dato: string, number, boolean | Sí |
| Descripción | Qué representa este parámetro | Sí |
| Valor por Defecto | Respaldo si no se proporciona | No |

### Tipos de Datos

<Tabs>
  <Tab title="String">
    Valores de texto como nombres, direcciones o identificadores.
    
    ```json
    {
      "nombre_cliente": "Juan García",
      "id_pedido": "PED-12345"
    }
    ```
  </Tab>
  <Tab title="Number">
    Valores numéricos como cantidades, importes o IDs.
    
    ```json
    {
      "saldo_cuenta": 1250.50,
      "cantidad_pedidos": 3
    }
    ```
  </Tab>
  <Tab title="Boolean">
    Valores verdadero/falso para flags y condiciones.
    
    ```json
    {
      "es_cliente_premium": true,
      "tiene_pago_pendiente": false
    }
    ```
  </Tab>
</Tabs>

## Usando Parámetros en Prompts

Una vez definidos, puedes referenciar parámetros de entrada en tu prompt del sistema y saludos usando la sintaxis `{{nombre_parametro}}`.

### Ejemplo: Saludo Personalizado

**Prompt del Sistema:**
```
Eres un agente de servicio al cliente para Acme Corp.
Estás hablando con {{nombre_cliente}}.
El estado de su cuenta es {{estado_cuenta}}.
```

**Saludo (Saliente):**
```
Hola {{nombre_cliente}}, soy Sara de Acme Corp. 
Le llamo sobre su pedido {{numero_pedido}}.
```

### Ejemplo: Comportamiento Condicional

```
{{#if es_cliente_premium}}
Trata a este cliente como VIP. Ofrece soporte prioritario y ofertas exclusivas.
{{else}}
Proporciona soporte estándar. Menciona opciones de mejora si es relevante.
{{/if}}
```

## Integración API

Al hacer llamadas API para iniciar llamadas, incluye parámetros de entrada en tu petición:

```bash
curl -X POST https://api.meetzy.io/v1/calls \
  -H "Authorization: Bearer TU_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "agent_id": "agente_123",
    "phone_number": "+34612345678",
    "input_params": {
      "nombre_cliente": "Juan García",
      "numero_pedido": "PED-12345",
      "es_cliente_premium": true,
      "saldo_cuenta": 1250.50
    }
  }'
```

## Probando Parámetros de Entrada

Usa el Playground para probar cómo tu agente maneja diferentes valores de parámetros:

<Steps>
  <Step title="Abrir Playground">
    Navega al Playground en el panel derecho.
  </Step>
  <Step title="Abrir Variables de Contexto">
    Haz clic en el panel "Variables de Contexto".
  </Step>
  <Step title="Establecer Valores de Prueba">
    Ingresa valores para cada parámetro de entrada.
  </Step>
  <Step title="Iniciar Conversación">
    Prueba cómo el agente usa estos valores.
  </Step>
</Steps>

## Mejores Prácticas

<Warning>
Usa nombres descriptivos y descripciones claras para que la IA entienda el contexto de cada parámetro.
</Warning>

1. **Usa Nombres Claros** - `nombre_cliente` es mejor que `nc`
2. **Proporciona Descripciones** - Ayuda a la IA a entender el contexto
3. **Establece Valores por Defecto** - Previene errores cuando no se proporcionan parámetros
4. **Valida Antes de Enviar** - Asegura que los parámetros tienen valores válidos
5. **Documenta para el Equipo** - Mantén una referencia de todos los parámetros y sus propósitos
6. **Acepta Sugerencias del Copiloto** - Las sugerencias de IA pueden mejorar la personalización

## Plantillas de Parámetros Comunes

### Información del Cliente
```json
{
  "nombre_cliente": "string",
  "telefono_cliente": "string", 
  "email_cliente": "string",
  "id_cliente": "string"
}
```

### Detalles de Cita
```json
{
  "fecha_cita": "string",
  "hora_cita": "string",
  "tipo_servicio": "string",
  "ubicacion": "string"
}
```

### Información de Pedido
```json
{
  "numero_pedido": "string",
  "total_pedido": "number",
  "cantidad_articulos": "number",
  "fecha_entrega": "string"
}
```

### Datos de Cuenta
```json
{
  "saldo_cuenta": "number",
  "tipo_cuenta": "string",
  "fecha_vencimiento": "string",
  "es_cliente_premium": "boolean"
}
```

## Flujo de Trabajo con IA Copiloto

<Steps>
  <Step title="Escribir Prompt del Sistema">
    Comienza escribiendo o editando tu prompt del sistema en el editor principal.
  </Step>
  <Step title="Obtener Sugerencias">
    El Copiloto analizará el prompt y sugerirá parámetros automáticamente.
  </Step>
  <Step title="Revisar Sugerencias">
    Examina las sugerencias en el banner púrpura - verifica nombres, tipos y descripciones.
  </Step>
  <Step title="Aceptar o Rechazar">
    Acepta las sugerencias útiles o recházalas si no son relevantes.
  </Step>
  <Step title="Ajustar si es Necesario">
    Edita manualmente los parámetros después de aceptar las sugerencias.
  </Step>
</Steps>

## Siguientes Pasos

Después de configurar tus parámetros de entrada:

- **Configura Parámetros de Salida** - Define qué información extraer de las conversaciones
- **Prueba en el Playground** - Simula llamadas con diferentes valores de parámetros
- **Configura Webhooks** - Recibe los datos extraídos en tu sistema