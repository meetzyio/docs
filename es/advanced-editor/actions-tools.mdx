---
title: 'Acciones y Herramientas'
description: 'Configura transferencias con reglas múltiples, detección temprana de buzón de voz, finalización de llamadas, webhooks, recall automático, horarios de atención, comunicación multicanal y dominios de email propios'
icon: 'wrench'
---

## Resumen

La sección de Acciones y Herramientas permite configurar funcionalidades avanzadas que potencian las capacidades de tu agente conversacional durante y después de las llamadas. Incluye transferencias telefónicas con reglas múltiples (Transfer v2 Beta), detección temprana y automática de contestadores, webhooks personalizados, protocolos de rellamada, el **Schedule Controller (Beta)** para horarios de atención, integración de calendario y comunicación multicanal con Email, SMS y WhatsApp, incluyendo la vinculación de tu propio dominio de email.

<Info>
Las funcionalidades configuradas aquí son clave para crear experiencias conversacionales fluidas y automatizaciones poderosas que integren con tus sistemas existentes, maximizando la eficiencia operacional y la satisfacción del cliente.
</Info>

## Transferencia de Llamadas

Configura cómo tu agente maneja las transferencias de llamada a agentes humanos, extensiones internas o sistemas telefónicos externos con plantillas multiidioma optimizadas.

<Info>
Meetzy ofrece dos versiones del sistema de transferencia: la versión clásica **v1** (destino único) y la nueva **Transfer v2 (Beta)**, que permite definir múltiples reglas de transferencia (`TransferRule`) con condiciones independientes. Puedes alternar entre ambas desde el interruptor "Transfer to multiple destinations (Beta)" en el panel de configuración; internamente esto se controla mediante la propiedad `transferConfigVersion` (`'v1' | 'v2'`), que por defecto es `'v1'` para no alterar el comportamiento de agentes ya existentes.
</Info>

### Tipos de Transferencia Disponibles

Meetzy soporta dos métodos principales de transferencia con configuración flexible:

<Tabs>
  <Tab title="Número de Teléfono">
    Transferir a números telefónicos tradicionales (móvil, fijo, internacional).
    
    **Formato requerido**: `+34600123456`
    
    **Casos de uso comunes**:
    - Escalar a agentes de soporte humano
    - Conectar con departamentos específicos
    - Transferir a supervisores o especialistas
    - Dirigir a líneas de emergencia
    - Routing geográfico por ubicación del cliente
  </Tab>
  <Tab title="SIP Trunk">
    Transferir a través del protocolo SIP para sistemas telefónicos empresariales.
    
    **Formato requerido**: `sip:extensión@gateway_ip:puerto`
    
    **Ejemplo**: `sip:100@78.136.102.246:5060`
    
    **Casos de uso comunes**:
    - Transferir a extensiones internas de PBX
    - Conectar con sistemas VoIP empresariales
    - Integrar con infraestructura telefónica existente
    - Enrutar a grupos de agentes específicos
    - Queue management inteligente
  </Tab>
</Tabs>

### Proceso de Configuración

<Steps>
  <Step title="Activar Transferencia">
    Habilita la opción de transferencia de llamadas usando el interruptor en la interfaz de usuario.
  </Step>
  <Step title="Elegir Versión del Sistema de Transferencia">
    Decide si usarás el modo clásico (`transferConfigVersion: 'v1'`, un único destino) o **Transfer v2 (Beta)** (`transferConfigVersion: 'v2'`), que permite configurar múltiples reglas con condiciones distintas.
  </Step>
  <Step title="Seleccionar Tipo de Transferencia">
    Elige entre "Número de Teléfono" o "SIP Trunk" según tu infraestructura de comunicaciones (esta elección se hace por cada regla en v2).
  </Step>
  <Step title="Configurar Destino">
    - Para números telefónicos: Introduce el número en formato E.164 (+país + número)
    - Para SIP: Especifica la URI completa con extensión, IP y puerto
  </Step>
  <Step title="Definir Plantilla de Transferencia">
    Selecciona una plantilla predefinida optimizada para tu idioma o crea una personalizada.
  </Step>
  <Step title="Validar Configuración">
    Prueba la configuración con llamadas de test para verificar conectividad.
  </Step>
</Steps>

### Transfer v2 (Beta): Reglas Múltiples de Transferencia

<Info>
**Transfer v2 (Beta)** es la evolución del sistema de transferencia clásico. En lugar de definir un único destino y una única plantilla de condiciones, permite crear **múltiples reglas independientes** (representadas internamente por la interfaz `TransferRule`), cada una con su propia condición de activación y su propio destino. Esto es ideal para agentes que deben enrutar llamadas a distintos departamentos, idiomas o niveles de prioridad según el motivo de la consulta.
</Info>

Para activar esta funcionalidad, utiliza el interruptor **"Transfer to multiple destinations (Beta)"** que aparece al habilitar la transferencia de llamadas. Al activarlo, la interfaz cambia de un formulario de destino único a una lista dinámica de reglas (`transferRules: TransferRule[]`).

#### Cómo Funciona una Regla de Transferencia

Cada regla (`TransferRule`) se compone de dos partes:

| Componente | Descripción | Ejemplo |
|-----------|-------------|---------|
| **Condición** | Frase en lenguaje natural que describe cuándo debe aplicarse la regla. La IA evalúa esta condición durante la conversación. | "el cliente pregunta por facturación o pagos" |
| **Destino** | Número de teléfono o URI SIP al que se transferirá la llamada cuando se cumpla la condición | `+34911234567` o `sip:200@gateway.empresa.com:5060` |

<Steps>
  <Step title="Activar Transfer v2">
    Activa el interruptor "Transfer to multiple destinations (Beta)" dentro de la sección de Call Transfer. Esto fija `transferConfigVersion` a `'v2'`.
  </Step>
  <Step title="Añadir Reglas">
    Haz clic en "Añadir Regla" para crear un nuevo elemento en el array `transferRules`. Puedes crear tantas reglas como necesites.
  </Step>
  <Step title="Definir la Condición">
    Escribe en el campo "When should this be used?" una descripción clara de la situación que debe activar esta regla (ej. "el cliente solicita cancelar su suscripción").
  </Step>
  <Step title="Elegir Tipo de Destino">
    Selecciona "Phone" o "SIP" para cada regla de forma independiente.
  </Step>
  <Step title="Configurar el Valor de Destino">
    Introduce el número en formato E.164 o la URI SIP completa según el tipo seleccionado.
  </Step>
  <Step title="Priorizar y Ordenar Reglas">
    Ordena las reglas de mayor a menor prioridad; la IA evaluará las condiciones en el orden establecido dentro del array `transferRules` y aplicará la primera que coincida.
  </Step>
  <Step title="Eliminar o Editar Reglas">
    Usa el botón "Remove" en cada tarjeta de regla para eliminarla, o edita directamente los campos para actualizarla.
  </Step>
</Steps>

#### Ejemplo de Configuración con Múltiples Reglas

```json
{
  "transferConfigVersion": "v2",
  "transferEnabled": true,
  "transferRules": [
    {
      "condition": "El cliente pregunta por facturación, pagos o cargos duplicados",
      "destination": {
        "type": "phone",
        "value": "+34911234567"
      }
    },
    {
      "condition": "El cliente solicita soporte técnico o reporta un problema con el producto",
      "destination": {
        "type": "sip",
        "value": "sip:200@gateway.empresa.com:5060"
      }
    },
    {
      "condition": "El cliente pide expresamente hablar con un supervisor o gerente",
      "destination": {
        "type": "phone",
        "value": "+34911234568"
      }
    },
    {
      "condition": "El cliente muestra alta frustración o el caso requiere atención urgente",
      "destination": {
        "type": "sip",
        "value": "sip:911@gateway.empresa.com:5060"
      }
    }
  ]
}
```

#### Transfer v1 vs Transfer v2 (Beta): Comparativa

| Característica | Transfer Clásico (v1) | Transfer v2 (Beta) |
|----------------|------------------------|---------------------|
| **Número de destinos** | Uno único | Múltiples, sin límite práctico (`TransferRule[]`) |
| **Condiciones** | Una plantilla general multiidioma | Una condición independiente por regla |
| **Enrutamiento por departamento** | No soportado nativamente | Soportado de forma nativa |
| **Priorización de reglas** | No aplica | Sí, orden configurable según posición en el array |
| **Complejidad de configuración** | Baja | Media (requiere definir cada regla) |
| **Casos de uso ideales** | Agentes con un único punto de escalado | Contact centers con múltiples departamentos o niveles de servicio |

<Warning>
Transfer v2 se encuentra en fase **Beta**. Es recomendable probar exhaustivamente cada regla con llamadas de test antes de desplegar en producción, especialmente cuando existen condiciones que podrían solaparse entre sí. En caso de solapamiento, la IA aplicará la primera regla que coincida según el orden configurado en `transferRules`.
</Warning>

#### Buenas Prácticas para Reglas de Transferencia

- **Condiciones mutuamente excluyentes**: Redacta las condiciones de forma que no se superpongan entre sí para evitar ambigüedad en el enrutamiento
- **Orden por especificidad**: Coloca las reglas más específicas primero y las más genéricas al final como fallback
- **Regla de respaldo (fallback)**: Considera añadir siempre una última regla genérica ("cualquier otra situación que requiera atención humana") apuntando a un destino general
- **Nombres descriptivos en la condición**: Usa lenguaje natural claro y verbos de acción ("el cliente solicita...", "el cliente pregunta por...") para maximizar la precisión de detección de la IA
- **Pruebas por regla**: Valida cada regla individualmente con llamadas de test simulando distintos escenarios de conversación
- **No mezclar versiones**: Si migras de v1 a v2, revisa que la plantilla de condición clásica quede reflejada como una o varias reglas equivalentes en `transferRules` antes de desactivar el modo clásico

### Plantillas de Transferencia Multiidioma

El sistema incluye plantillas predefinidas optimizadas para diferentes idiomas y contextos culturales:

<Tabs>
  <Tab title="Español">
    ```text
    Se usa para transferir la llamada con una persona en los casos que se indican:
    
    - El usuario rechaza directamente la propuesta o servicio
    - El usuario solicita expresamente hablar con una persona humana
    - El usuario muestra desconfianza hacia el asistente virtual
    - Se necesita información específica no disponible en el sistema
    - Se requiere autorización o gestión de datos personales sensibles
    - El cliente expresa frustración o necesita atención personalizada
    - La consulta requiere conocimiento técnico especializado
    - Se solicita hablar con un supervisor o responsable
    - El caso requiere decisiones comerciales o excepciones
    - Se detecta alta complejidad emocional o sensibilidad
    ```
  </Tab>
  <Tab title="Inglés">
    ```text
    Used to transfer the call to a person in the following cases:
    
    - The user directly rejects the proposal or service
    - The user expressly requests to speak with a human person
    - The user shows distrust towards the virtual assistant
    - Specific information is needed that is not available in the system
    - Authorization or management of sensitive personal data is required
    - The client expresses frustration or needs personalized attention
    - The query requires specialized technical knowledge
    - Request to speak with a supervisor or manager
    - The case requires business decisions or exceptions
    - High emotional complexity or sensitivity is detected
    ```
  </Tab>
  <Tab title="Francés">
    ```text
    Utilisé pour transférer l'appel à une personne dans les cas indiqués :
    
    - L'utilisateur rejette directement la proposition ou le service
    - L'utilisateur demande expressément à parler avec une personne humaine
    - L'utilisateur montre de la méfiance envers l'assistant virtuel
    - Des informations spécifiques sont nécessaires qui ne sont pas disponibles
    - L'utilisateur nécessite une autorisation ou gestion de données sensibles
    - Le client exprime sa frustration ou a besoin d'attention personnalisée
    - La demande nécessite des connaissances techniques spécialisées
    - Demande de parler avec un superviseur ou responsable
    - Le cas nécessite des décisions commerciales ou des exceptions
    - Une complexité émotionnelle élevée ou sensibilité est détectée
    ```
  </Tab>
  <Tab title="Alemán">
    ```text
    Wird verwendet, um das Gespräch in folgenden Fällen an eine Person zu übertragen:
    
    - Der Benutzer lehnt den Vorschlag oder Service direkt ab
    - Der Benutzer möchte ausdrücklich mit einer echten Person sprechen
    - Der Benutzer zeigt Misstrauen gegenüber dem virtuellen Assistenten
    - Spezifische Informationen werden benötigt, die nicht verfügbar sind
    - Der Benutzer benötigt Autorisierung oder sensible Datenverwaltung
    - Der Kunde äußert Frustration oder benötigt persönliche Betreuung
    - Die Anfrage erfordert spezialisiertes technisches Wissen
    - Anfrage, mit einem Supervisor oder Manager zu sprechen
    - Der Fall erfordert Geschäftsentscheidungen oder Ausnahmen
    - Hohe emotionale Komplexität oder Sensibilität wird erkannt
    ```
  </Tab>
  <Tab title="Italiano">
    ```text
    Si usa per trasferire la chiamata a una persona nei casi indicati:
    
    - L'utente rifiuta direttamente la proposta o il servizio
    - L'utente richiede espressamente di parlare con una persona umana
    - L'utente mostra sfiducia verso l'assistente virtuale
    - Sono necessarie informazioni specifiche non disponibili nel sistema
    - L'utente richiede autorizzazione o gestione di dati personali sensibili
    - Il cliente esprime frustrazione o necessita di attenzione personalizzata
    - La richiesta richiede conoscenza tecnica specializzata
    - Si richiede di parlare con un supervisore o responsabile
    - Il caso richiede decisioni commerciali o eccezioni
    - Viene rilevata alta complessità emotiva o sensibilità
    ```
  </Tab>
  <Tab title="Portugués">
    ```text
    É usado para transferir a chamada para uma pessoa nos casos indicados:
    
    - O usuário rejeita diretamente a proposta ou serviço
    - O usuário solicita expressamente falar com uma pessoa humana
    - O usuário mostra desconfiança em relação ao assistente virtual
    - São necessárias informações específicas não disponíveis no sistema
    - O usuário requer autorização ou gestão de dados pessoais sensíveis
    - O cliente expressa frustração ou precisa de atenção personalizada
    - A consulta requer conhecimento técnico especializado
    - Solicita falar com um supervisor ou responsável
    - O caso requer decisões comerciais ou exceções
    - É detectada alta complexidade emocional ou sensibilidade
    ```
  </Tab>
</Tabs>

## Estrategia de Finalización de Llamada

Configura cómo y cuándo tu agente debe finalizar automáticamente las conversaciones para optimizar recursos y mejorar la experiencia del usuario con detección inteligente multiidioma.

### Detección Automática de Contestadores

Una funcionalidad crítica es la detección automática de contestadores telefónicos para evitar conversaciones con sistemas automatizados:

<Warning>
La detección de contestadores automáticos es esencial para evitar conversaciones innecesarias y optimizar costos operacionales. Esta funcionalidad previene la frustración de interacciones con buzones de voz y sistemas automatizados, mejorando significativamente el ROI de campañas.
</Warning>

#### Frases de Detección por Idioma

El sistema AI reconoce patrones específicos en cada idioma con alta precisión:

<Tabs>
  <Tab title="Español">
    ```text
    Frases detectadas automáticamente:
    - "En este momento no podemos atenderle"
    - "Deje su mensaje después de la señal"
    - "El número al que llama no está disponible"
    - "Ha llamado al buzón de..."
    - "Este es el contestador automático de..."
    - "Gracias por llamar, en estos momentos..."
    - "Bienvenido al buzón de voz de..."
    - "Puedes dejar tu mensaje al oír la señal"
    - "Lo sentimos, pero en estos momentos..."
    - "Por favor, inténtelo de nuevo más tarde"
    - "No podemos atender su llamada ahora"
    - "Ha contactado con el servicio de..."
    - "Este es el servicio de mensajería de..."
    - "Deje su recado después del pitido"
    ```
  </Tab>
  <Tab title="Inglés">
    ```text
    Frases detectadas automáticamente:
    - "Please leave your message after the tone"
    - "We're sorry, but we cannot take your call..."
    - "The person you're calling is not available"
    - "You have reached the voicemail of..."
    - "Thank you for calling, we are currently..."
    - "Your call is important to us, please hold..."
    - "We cannot take your call right now"
    - "Leave your message at the sound of the beep"
    - "This is the answering machine of..."
    - "Please try your call again later"
    - "You've reached the voice mailbox of..."
    - "The number you have dialed is not available"
    ```
  </Tab>
  <Tab title="Francés">
    ```text
    Frases detectadas automáticamente:
    - "Veuillez laisser votre message après le signal"
    - "Nous sommes désolés, mais en ce moment..."
    - "La personne que vous appelez n'est pas disponible"
    - "Vous avez joint la messagerie vocale de..."
    - "Merci d'avoir appelé, nous sommes actuellement..."
    - "Bienvenue sur le répondeur de..."
    - "Laissez votre message après le bip"
    - "Vous êtes bien sur la messagerie de..."
    - "Veuillez rappeler plus tard"
    - "Ce numéro n'est pas disponible actuellement"
    ```
  </Tab>
  <Tab title="Alemán">
    ```text
    Frases detectadas automáticamente:
    - "Bitte hinterlassen Sie Ihre Nachricht nach dem Ton"
    - "Es tut uns leid, aber zur Zeit können wir..."
    - "Die Person, die Sie anrufen, ist nicht verfügbar"
    - "Sie haben die Mailbox von... erreicht"
    - "Vielen Dank für Ihren Anruf, wir sind derzeit..."
    - "Willkommen beim Anrufbeantworter von..."
    - "Hinterlassen Sie Ihre Nachricht nach dem Piepton"
    - "Sie sind mit der Sprachnachricht von... verbunden"
    - "Bitte versuchen Sie es später erneut"
    ```
  </Tab>
  <Tab title="Italiano">
    ```text
    Frases detectadas automáticamente:
    - "Per favore lasci il suo messaggio dopo il tono"
    - "Ci dispiace, ma al momento non possiamo..."
    - "La persona che sta chiamando non è disponibile"
    - "Ha raggiunto la segreteria di..."
    - "Grazie per aver chiamato, al momento siamo..."
    - "Benvenuto alla segreteria telefonica di..."
    - "Lasci il suo messaggio dopo il segnale acustico"
    - "È collegato con la segreteria di..."
    - "La preghiamo di richiamare più tardi"
    ```
  </Tab>
  <Tab title="Portugués">
    ```text
    Frases detectadas automáticamente:
    - "Por favor deixe sua mensagem após o sinal"
    - "Lamentamos, mas no momento não podemos..."
    - "A pessoa que você está ligando não está disponível"
    - "Você chegou à caixa postal de..."
    - "Obrigado por ligar, no momento estamos..."
    - "Bem-vindo à secretária eletrônica de..."
    - "Deixe sua mensagem após o bip"
    - "Você está conectado com a caixa postal de..."
    - "Por favor, tente ligar novamente mais tarde"
    ```
  </Tab>
</Tabs>

### Detección Temprana de Buzón de Voz (Early Voicemail Detection)

<Info>
Además de la detección conversacional realizada por la IA (descrita arriba), Meetzy incorpora una capa de **Detección Temprana de Buzón de Voz** que opera a nivel de operador telefónico (carrier), permitiendo identificar contestadores automáticos *antes* de que el agente empiece a hablar, o en paralelo mientras lo hace. Esta funcionalidad se controla mediante la propiedad `amd_mode` (`'' | 'off' | 'live' | 'gate'`) y se expone en la interfaz como un interruptor **"Voicemail Detection"** junto a un selector de modo.
</Info>

#### Dos Niveles de Detección Complementarios

| Nivel | Cómo Funciona | Cuándo Actúa | Configuración |
|-------|---------------|--------------|----------------|
| **Nativo (Agente/IA)** | La IA analiza el contenido semántico de lo que escucha y reconoce frases típicas de contestadores | Durante la conversación, una vez que el agente ya ha empezado a interactuar | `voicemailDetectionMode: 'native'` (comportamiento por defecto) |
| **Operador (Carrier)** | El propio operador de telefonía analiza señales técnicas de la llamada (tonos, patrones de silencio, señal de "beep") para determinar si es un contestador | Al inicio de la llamada, antes o en paralelo al primer mensaje del agente | `voicemailDetectionMode: 'gate'` o `'live'` |

#### Modos de Detección a Nivel de Operador

Cuando seleccionas la detección a nivel de "Carrier", puedes elegir entre dos submodos, cada uno con un compromiso distinto entre velocidad de respuesta y precisión:

<Tabs>
  <Tab title="Gate (Puerta de Espera)">
    En modo **Gate**, el sistema **retiene la voz del agente** hasta que el operador telefónico confirma si la llamada fue contestada por una persona o por un contestador automático. Solo cuando se confirma que hay una persona al otro lado, el agente comienza a hablar.

    **Ventajas**:
    - Máxima precisión: el agente nunca "malgasta" su primer mensaje hablándole a un contestador
    - Ideal para mensajes de apertura largos o con información sensible

    **Consideraciones**:
    - Introduce una pequeña latencia inicial mientras se espera la confirmación del operador
    - Recomendado para campañas salientes de alto volumen donde el coste de "desperdiciar" aperturas es alto
  </Tab>
  <Tab title="Live (Habla en Vivo)">
    En modo **Live**, el agente **comienza a hablar de inmediato** al descolgarse la llamada, sin esperar confirmación del operador. En paralelo, el sistema monitoriza la llamada y, si detecta que se trata de un contestador automático, puede finalizar la llamada automáticamente.

    **Ventajas**:
    - Sin latencia inicial: la conversación se siente más natural y humana desde el primer segundo
    - Ideal para llamadas entrantes donde la inmediatez es crítica para la experiencia del usuario

    **Consideraciones**:
    - Existe una pequeña ventana de tiempo donde el agente podría empezar a hablar antes de que se confirme que es un contestador
    - Se recomienda combinar con la detección nativa (IA) como capa de respaldo
  </Tab>
</Tabs>

#### Proceso de Configuración

<Steps>
  <Step title="Activar Voicemail Detection">
    Habilita el interruptor "Voicemail Detection" en la sección de Acciones y Herramientas (`voicemailDetectionEnabled: true`).
  </Step>
  <Step title="Elegir el Nivel de Detección">
    Selecciona entre "Agent" (detección nativa realizada por la IA durante la conversación) o "Carrier" (detección técnica a nivel de operador telefónico).
  </Step>
  <Step title="Configurar Submodo de Carrier (si aplica)">
    Si eliges "Carrier", selecciona entre "Gate" (esperar confirmación antes de hablar) o "Live" (hablar de inmediato y monitorizar en paralelo).
  </Step>
  <Step title="Validar con Llamadas de Prueba">
    Realiza llamadas de test tanto a números que sean contestados por personas como a números con contestador automático para verificar el comportamiento esperado en cada escenario.
  </Step>
</Steps>

<Info>
El sistema recuerda automáticamente el último submodo de Carrier utilizado (`lastCarrierVoicemailMode: 'gate' | 'live'`). Así, si cambias temporalmente a detección "Agent" y luego regresas a "Carrier", la interfaz restaura el submodo que tenías configurado previamente, evitando que tengas que reconfigurarlo cada vez.
</Info>

#### Comparativa de Modos de Detección

| Modo | Momento de Detección | Latencia Inicial | Precisión | Recomendado Para |
|------|----------------------|-------------------|-----------|-------------------|
| **Native (Agent)** | Durante la conversación, vía análisis semántico de la IA | Ninguna | Alta, pero requiere que el contestador haya reproducido su mensaje | Llamadas entrantes y salientes con mensajes de apertura cortos |
| **Carrier - Gate** | Antes de que el agente hable, vía señal técnica del operador | Baja a moderada | Muy alta | Campañas salientes masivas con mensajes de apertura largos o sensibles |
| **Carrier - Live** | En paralelo al primer mensaje del agente | Ninguna | Alta, con posible margen de error mínimo al inicio | Llamadas entrantes donde la inmediatez es prioritaria |

<Warning>
La disponibilidad de los modos "Gate" y "Live" depende de las capacidades de detección de contestadores (AMD) ofrecidas por tu proveedor de telefonía/carrier. Verifica con tu proveedor qué modos están soportados en tu configuración de troncal SIP antes de activarlos en producción.
</Warning>

#### Buenas Prácticas

- **Combina niveles**: Usa la detección "Carrier" como primera línea de defensa y mantén la detección "Native" activa como respaldo semántico
- **Elige el submodo según el caso de uso**: "Gate" para campañas salientes masivas; "Live" para llamadas entrantes donde la naturalidad es crítica
- **Prueba con distintos operadores**: Si trabajas con múltiples troncales SIP, valida el comportamiento de AMD con cada proveedor, ya que la precisión puede variar
- **Monitorea la tasa de falsos positivos**: Revisa periódicamente las llamadas finalizadas automáticamente por detección de contestador para asegurarte de que no se estén cortando conversaciones con personas reales

### Plantillas de Finalización por Idioma

Configura respuestas inteligentes de finalización según el idioma detectado:

<Tabs>
  <Tab title="Español">
    ```text
    ## Finalización de llamada por Detección de Contestador Automático

    Si se detecta que el usuario es un contestador automático, finaliza la llamada de inmediato.

    ## Finalización de Llamada por otros motivos: 

    También debes finalizar las llamadas cuando:
    - El usuario se despida de forma clara y definitiva
    - Te despidas tú mismo tras completar el objetivo
    - Se alcancen todos los objetivos principales establecidos
    - El usuario solicite expresamente finalizar la conversación
    - Se detecte hostilidad persistente o abuso verbal
    - La llamada sea colgada por problemas técnicos
    ```
  </Tab>
  <Tab title="Inglés">
    ```text
    ## Call Termination by Answering Machine Detection

    If it is detected that the user is an answering machine, terminate the call immediately.

    ## Call Termination for other reasons:

    You should also terminate calls when:
    - The user clearly and definitively says goodbye
    - You say goodbye yourself after completing the objective
    - All main established objectives are achieved
    - The user expressly requests to end the conversation
    - Persistent hostility or verbal abuse is detected
    - The call is dropped due to technical problems
    ```
  </Tab>
  <Tab title="Francés">
    ```text
    ## Finalisation d'appel par Détection de Répondeur Automatique

    Si il est détecté que l'utilisateur est un répondeur automatique, finalisez l'appel immédiatement.

    ## Finalisation d'Appel pour d'autres raisons :

    Vous devez également finaliser les appels quand :
    - L'utilisateur dit clairement et définitivement au revoir
    - Vous dites au revoir vous-même après avoir terminé l'objectif
    - Tous les objectifs principaux établis sont atteints
    - L'utilisateur demande expressément de terminer la conversation
    - Une hostilité persistante ou des abus verbaux sont détectés
    - L'appel est raccroché à cause de problèmes techniques
    ```
  </Tab>
</Tabs>

### Condiciones Adicionales de Finalización

El agente finalizará llamadas automáticamente en estas situaciones optimizadas:

- **Despedida Natural**: El usuario se despide claramente y el agente completa protocolarmente su despedida
- **Objetivos Completados**: Se han alcanzado exitosamente todos los objetivos específicos de la conversación
- **Timeout Configurado**: Se supera el tiempo máximo establecido para la duración de la llamada (configurable por agente)
- **Detección de Silencio Prolongado**: Períodos extendidos sin respuesta audible del interlocutor (umbral ajustable)
- **Interrupción de Línea**: Llamada cortada inesperadamente por problemas técnicos o cuelgue del cliente
- **Transferencia Exitosa**: Después de completar satisfactoriamente la transferencia a un agente humano
- **Detección de Hostilidad**: Cuando se identifica agresividad sostenida o abuso verbal del interlocutor
- **Compliance Violation**: Si se detecta solicitud de opt-out o violación de políticas de comunicación

## Schedule Controller (Beta)

<Info>
**Schedule Controller (Beta)** es la nueva herramienta del editor avanzado (respaldada internamente por las interfaces `ScheduleController`, `ScheduleDayConfig` y `ScheduleHoliday`) que permite que tu agente conozca en tiempo real si la llamada se está produciendo dentro o fuera del horario comercial configurado, y actúe en consecuencia (informar al usuario, ofrecer alternativas, activar un recall, o transferir a otro destino según el horario).
</Info>

### ¿Qué Resuelve Esta Herramienta?

Antes de esta funcionalidad, gestionar el horario de atención requería lógica externa vía webhooks personalizados. Ahora, el Schedule Controller se integra de forma nativa en el editor avanzado como una herramienta más que la IA puede consultar durante la conversación, sin necesidad de configurar infraestructura adicional.

**Casos de uso principales:**

- Informar al cliente si está llamando fuera de horario y comunicar el próximo horario de apertura
- Evitar transferencias a agentes humanos cuando el departamento está cerrado
- Activar automáticamente el protocolo de Recall para reintentar la llamada en horario laboral
- Ofrecer un mensaje diferenciado según sea horario laboral, fin de semana o festivo
- Enrutar a un buzón de mensajes o a un flujo de auto-servicio fuera de horario

### Modelo de Datos: `ScheduleController`

El Schedule Controller se construye sobre tres piezas de configuración claramente separadas, igual que un experimento correctamente controlado necesita variables independientes bien definidas:

| Interfaz | Responsabilidad | Ejemplo de Contenido |
|----------|------------------|------------------------|
| **`ScheduleController`** | Objeto raíz: activación, zona horaria y comportamiento fuera de horario | `enabled`, `timezone`, `outOfHoursBehavior` |
| **`ScheduleDayConfig`** | Configura un día concreto de la semana (abierto/cerrado, hora de inicio y fin) | `{ open: "09:00", close: "18:00" }` o `{ closed: true }` |
| **`ScheduleHoliday`** | Excepción puntual que sobrescribe el horario habitual en una fecha específica | `{ date: "2025-12-25", closed: true